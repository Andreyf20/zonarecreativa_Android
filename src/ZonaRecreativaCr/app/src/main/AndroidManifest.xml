<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.zona.recreativacr">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".PackagesAddActivity"></activity>
        <activity android:name=".MedicalStaffsAddActivity" />
        <activity android:name=".MealPlansAddActivity" />
        <activity android:name=".TransportsAddActivity" />
        <activity android:name=".EmployeesAddActivity" />
        <activity android:name=".TransportsActivity" />
        <activity android:name=".MedicalStaffsActivity" />
        <activity android:name=".MealPlansActivity" />
        <activity
            android:name=".EmployeesActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".PackagesActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".NewAdminActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".HomeActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>